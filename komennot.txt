komennot ja bugit

cd C:\ajx\ajx-ai
taskkill /F /IM node.exe
npm run build
npx next start --hostname 192.168.100.88 --port 3000

cd C:\ajx\ajx-ai
taskkill /F /IM node.exe
npm run build
npx next start --hostname 192.168.100.88 --port 3000

cd C:\ajx\ajx-ai
taskkill /F /IM node.exe
rmdir .next /s /q
npm run build
npx next start --hostname 192.168.100.88 --port 3000

cd C:\ajx\ajx-ai

# tappaa node jos päällä (ei kaadu jos ei löydy)
taskkill /F /IM node.exe 2>$null

# poista build cache turvallisesti
if (Test-Path .next) {
  Remove-Item .next -Recurse -Force
}

# (varmistus) riippuvuudet
npm install

# build
npm run build

# käynnistys LAN:iin
npx next start --hostname 192.168.100.88 --port 3000


bugi lista 2

- suurin ongelma on että vastaukset tulevat yhdessä pötkössä
- verkkohaku ei toimi kunnolla. sää jutut kyllä toimii mutta mikään muu ei. pitäisi saada sillä tavalla että haku pakotettaisiin jos sitä käytetään.
- pro tasolla ainakin päivämäärä ei täsmää viikonpäivän kanssa. Company tasolla täsmää. miksi?

- tuote verkkoon testaukseen ihmisille ennen julkistusta

- mobiili aplikaatio
- miten saamme saman käyttäjän tietokoneelle ja puhelimeen? joku profiili juttu pitäisi olla?
- maksusilta
- verkkosivut
- miten teemmme päivityksiä tulevaisuudessa?

Pieni vinkki jatkoon:
Koska käytät Gemini 3 Flashia, muista hyödyntää sen Context Cachingia. Jos käyttäjäsi alkavat tehdä pitkiä chat-ketjuja, voit tallentaa keskustelun historian välimuistiin. Se pudottaa toistuvien viestien kustannukset lähes nollaan ja tekee vastauksista vieläkin nopeampia.

